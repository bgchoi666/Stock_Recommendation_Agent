# 빠른 시작 가이드 (Quick Start Guide)

## 📦 설치 (5분)

### 1단계: Python 가상환경 생성
```bash
# 가상환경 생성
python -m venv venv

# 가상환경 활성화
# Windows:
venv\Scripts\activate

# Mac/Linux:
source venv/bin/activate
```

### 2단계: 패키지 설치
```bash
pip install -r requirements.txt
```

### 3단계: 파일 확인
```
프로젝트 폴더에 다음 파일들이 있는지 확인:
✓ stock_news_analyzer.py
✓ buy_list.csv
✓ 코스피200.csv
✓ 코스닥150.csv
✓ requirements.txt
```

---

## 🚀 첫 실행 (10분)

### Step 1: 뉴스 데이터 수집

전날 상승한 종목의 뉴스를 수집합니다.

```bash
python stock_news_analyzer.py crawl \
  --start 2026 1 15 \
  --end 2026 1 15 \
  --file buy_list.csv \
  --threshold 10
```

**예상 소요시간**: 3~5분 (종목 수에 따라)

**결과**: `급등락뉴스.csv` 파일 생성

---

### Step 2: 모델 학습

수집한 뉴스로 BERT 모델을 학습합니다.

```bash
python stock_news_analyzer.py train
```

**예상 소요시간**: 5~10분 (하드웨어에 따라)

**결과**: 
- `model_weights.h5` (학습된 모델)
- `training_history.png` (학습 그래프)

---

### Step 3: 종목 추천

학습된 모델로 추천 종목을 찾습니다.

```bash
python stock_news_analyzer.py recommend --list all --threshold 0.8
```

**예상 소요시간**: 2~3분

**결과**: `임의기간상승.csv` (추천 종목 리스트)

---

## 📊 결과 확인

### 추천 종목 확인
```bash
# CSV 파일 열기
cat 임의기간상승.csv

# 또는 엑셀로 열기
```

결과 예시:
```
date,item,news,result
2026-02-09-14-30,삼성전자,"반도체 수요 급증...",0.8542
2026-02-09-14-30,SK하이닉스,"실적 개선 기대...",0.8231
```

---

## 💡 일일 사용 워크플로우

### 매일 오전 루틴 (권장)

```bash
# 1. 전날 뉴스 수집 (매일)
python stock_news_analyzer.py crawl \
  --start $(date -d "yesterday" +%Y) $(date -d "yesterday" +%m) $(date -d "yesterday" +%d) \
  --end $(date -d "yesterday" +%Y) $(date -d "yesterday" +%m) $(date -d "yesterday" +%d) \
  --file buy_list.csv \
  --threshold 5

# 2. 모델 재학습 (주 1회 - 금요일 권장)
# 금요일에만 실행
python stock_news_analyzer.py train

# 3. 당일 종목 추천 (매일)
python stock_news_analyzer.py recommend --list all --threshold 0.8
```

---

## 🎯 사용 예시

### 예시 1: 코스피 200 종목만 분석
```bash
python stock_news_analyzer.py recommend --list kospi --threshold 0.85
```

### 예시 2: 특정 종목만 분석
```bash
python stock_news_analyzer.py recommend \
  --stocks 삼성전자 SK하이닉스 네이버 카카오 \
  --threshold 0.7
```

### 예시 3: 하락 종목 뉴스 수집
```bash
python stock_news_analyzer.py crawl \
  --start 2026 1 10 \
  --end 2026 1 10 \
  --file 코스피200.csv \
  --threshold -5
```

---

## ⚙️ 설정 조정

### 추천 임계값 변경
- **보수적 (엄격)**: `--threshold 0.9`
- **표준**: `--threshold 0.8`
- **공격적**: `--threshold 0.7`

### 학습 데이터 크기 조정
```bash
# 더 많은 데이터로 학습
python stock_news_analyzer.py train --train-size 100000
```

---

## ❓ 문제 해결

### 문제: 뉴스가 수집되지 않음
```
✓ 인터넷 연결 확인
✓ 종목명이 정확한지 확인
✓ 날짜 범위가 올바른지 확인
```

### 문제: 메모리 부족 오류
```python
# stock_news_analyzer.py에서 수정
Config.BATCH_SIZE = 16  # 기본값 32에서 감소
```

### 문제: 모델 가중치 파일 없음
```
✓ train 명령으로 먼저 학습 필요
✓ model_weights.h5 파일이 있는지 확인
```

---

## 📈 성능 향상 팁

### 1. GPU 사용 (10배 이상 빠름)
```bash
# CUDA 설치 후
pip install tensorflow-gpu==2.12.0
```

### 2. 더 많은 뉴스 수집
```bash
# 여러 날짜의 뉴스 수집
for day in {10..15}; do
  python stock_news_analyzer.py crawl \
    --start 2026 1 $day \
    --end 2026 1 $day \
    --file buy_list.csv \
    --threshold 5
done
```

### 3. 더 긴 학습
```python
# stock_news_analyzer.py에서 수정
Config.EPOCHS = 3  # 기본값 1에서 증가
```

---

## 🔔 중요 참고사항

### 데이터 수집 권장사항
- **최소 데이터**: 10,000건 이상
- **권장 데이터**: 50,000건 이상
- **이상적**: 100,000건 이상

### 학습 주기
- **처음**: 충분한 데이터 확보 후 1회 학습
- **이후**: 주 1회 재학습 권장
- **특별**: 시장 급변 시 즉시 재학습

### 투자 주의사항
⚠️ **이 프로그램은 투자 참고용입니다**
- 실제 투자는 본인의 판단과 책임하에
- 과거 데이터가 미래를 보장하지 않음
- 다양한 정보를 종합적으로 고려

---

## 📚 추가 학습 자료

### 상세 문서
- `사용설명서.md`: 전체 기능 설명
- `stock_news_analyzer.py`: 소스 코드 주석 참고

### 도움말 보기
```bash
# 전체 도움말
python stock_news_analyzer.py -h

# 특정 명령 도움말
python stock_news_analyzer.py crawl -h
python stock_news_analyzer.py train -h
python stock_news_analyzer.py recommend -h
```

---

## ✅ 체크리스트

시작하기 전에 확인하세요:

- [ ] Python 3.7+ 설치됨
- [ ] 가상환경 생성 및 활성화
- [ ] requirements.txt로 패키지 설치
- [ ] CSV 파일들이 준비됨
- [ ] 인터넷 연결 확인

첫 실행 완료 확인:

- [ ] 뉴스 수집 완료 (급등락뉴스.csv)
- [ ] 모델 학습 완료 (model_weights.h5)
- [ ] 종목 추천 완료 (임의기간상승.csv)

---

## 🎓 학습 경로

### 초급 (1주차)
1. 기본 명령어로 뉴스 수집
2. 모델 학습 실행
3. 추천 결과 확인

### 중급 (2~3주차)
1. 다양한 임계값 실험
2. 여러 기간의 데이터 수집
3. 결과 백테스팅

### 고급 (1개월 이후)
1. Config 클래스 커스터마이징
2. 자동화 스크립트 작성
3. 크론탭 등록으로 완전 자동화

---

**지금 바로 시작하세요! 🚀**

```bash
# Step 1
python stock_news_analyzer.py crawl --start 2026 1 15 --end 2026 1 15 --file buy_list.csv --threshold 10

# Step 2
python stock_news_analyzer.py train

# Step 3
python stock_news_analyzer.py recommend --list all --threshold 0.8
```
