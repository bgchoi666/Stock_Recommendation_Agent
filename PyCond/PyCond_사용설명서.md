# PyCond 프로그램 상세 설명서

## 1. 개요

PyCond는 키움증권 OpenAPI를 활용한 주식 종목 조건 검색 및 매매 관리 프로그램입니다. PyQt5 GUI를 통해 사용자 친화적인 인터페이스를 제공하며, 조건식 검색, 종목 분석, 매수/매도 리스트 관리 기능을 제공합니다.

### 주요 기능
- 키움 조건식을 이용한 종목 검색
- 여러 조건식의 교집합 종목 추출
- 보유종목 조회 및 관리
- 매수/매도 종목 리스트 생성 및 관리
- 주요 재무지표(PER, PBR, ROE) 조회
- 종목별 가격 정보(현재가, 연중최고가, 연중최저가) 조회
- 자동 종목 검색 및 CSV 저장 기능

## 2. 시스템 요구사항

### 필수 라이브러리
```python
- PyQt5: GUI 프레임워크
- pandas: 데이터 처리
- numpy: 수치 연산
- datetime: 날짜/시간 처리
- Kiwoom: 키움증권 OpenAPI 래퍼 (별도 모듈 필요)
```

### 필수 파일
- `condition.ui`: PyQt5 UI 파일
- `Kiwoom.py`: 키움 API 연동 모듈
- `buy_list.txt`: 매수 종목 리스트
- `sell_list.txt`: 매도 종목 리스트
- `searched_items.csv`: 검색된 종목 저장 파일
- `buy_list.csv`: 초기 매수 리스트 템플릿

## 3. 클래스 구조

### MyWindow 클래스
메인 윈도우 클래스로 QMainWindow와 UI 폼 클래스를 상속합니다.

#### 초기화 파라미터
- `gubun`: 프로그램 동작 모드 설정
  - 'exit': 자동 종목 검색 후 종료
  - 기타: 일반 GUI 모드

## 4. 주요 기능 설명

### 4.1 조건식 관리

#### show_condition_name_list()
- 키움 서버에서 가져온 조건식 목록을 리스트에 표시
- 보유종목, buy_list, sell_list가 자동으로 추가됨

#### select_cond_item()
- 사용자가 선택한 조건식을 검색 조건 리스트에 추가

#### refresh()
- 조건식 목록 새로고침
- 보유종목 정보를 다시 불러옴

### 4.2 종목 검색

#### search_items()
- 선택된 여러 조건식의 교집합 종목을 검색
- 검색 결과를 테이블에 표시

#### get_codes(index_list)
- 여러 조건식의 공통 종목 코드를 추출
- **알고리즘**: 첫 번째 조건식의 각 종목이 다른 모든 조건식에도 포함되어 있는지 확인

#### load_search_items(codes)
- 검색된 종목의 상세 정보를 테이블에 로드
- 종목코드, 종목명, PER, PBR, ROE 정보 표시

### 4.3 종목 정보 조회

#### get_item_info()
- 테이블에서 선택한 종목의 상세 정보 조회
- 두 가지 모드로 전환:
  - **모드 0**: 현재가, 연중최고가, 연중최저가
  - **모드 1**: PER, PBR, ROE

### 4.4 매수 리스트 관리

#### init_buy_list()
- buy_list.csv 파일을 읽어서 buy_list.txt 생성
- 파일 형식: `매수;종목코드;시장가;10;0;매수전;종목명`

#### add_buy_list()
- 현재 선택된 종목을 매수 리스트에 추가
- 추가 후 자동으로 정렬

#### reset_buy_list()
- 테이블의 모든 종목으로 매수 리스트를 재설정
- 기존 리스트를 덮어씀

#### load_buy_list()
- buy_list.txt 파일의 종목들을 테이블에 로드
- 각 종목의 재무지표 정보도 함께 조회

#### sort_buy_list()
- 매수 리스트를 평가 점수로 정렬
- **평가 공식**: `PER - ROE + PBR` (낮을수록 우선순위 높음)
- 재무지표가 없는 종목은 999점으로 처리되어 후순위로 이동

### 4.5 매도 리스트 관리

#### add_sell_list()
- 선택된 종목을 매도 리스트에 추가

#### reset_sell_list()
- 테이블의 모든 종목으로 매도 리스트를 재설정

#### load_sell_list()
- sell_list.txt 파일의 종목들을 테이블에 로드

#### sort_sell_list()
- 매도 리스트를 평가 점수로 정렬
- **평가 공식**: `PER - ROE + PBR` (높을수록 우선순위 높음, 내림차순)

### 4.6 보유종목 관리

#### holding_items()
- 키움 계좌에서 현재 보유중인 종목 조회
- opw00018 TR을 사용하여 보유 종목 정보 가져옴
- 계좌번호 뒤 두자리가 '11' 또는 '10'인 계좌 우선 선택
- 조회 결과를 condCodes 리스트에 추가

### 4.7 자동 검색 기능

#### load_search_all_items()
- 매수 조건과 다른 조건들의 교집합을 자동으로 검색
- 인덱스 0 (매수 조건)과 인덱스 2~10의 각 조건을 조합
- 검색 결과를 searched_items.csv에 저장
- 뉴스 분석 프로그램 자동 실행 (BERT 모델 활용)

### 4.8 UI 관리

#### delete_item()
- 선택 조건 리스트에서 항목 삭제

#### delete_item2()
- 검색 결과 테이블에서 행 삭제

#### clear_selection()
- 선택 조건 리스트 전체 초기화

#### timeout()
- 1초마다 호출되는 타이머 함수
- gubun이 'exit'인 경우 자동 검색 후 프로그램 종료

## 5. 버튼 매핑

| 버튼 | 기능 |
|------|------|
| pushButton | 조건식 선택 (select_cond_item) |
| pushButton_2 | 새로고침 (refresh) |
| pushButton_3 | 종목 검색 (search_items) |
| pushButton_4 | 선택 조건 초기화 (clear_selection) |
| pushButton_5 | 선택 조건 삭제 (delete_item) |
| pushButton_6 | 테이블 항목 삭제 (delete_item2) |
| pushButton_7 | 매수 리스트 추가 (add_buy_list) |
| pushButton_8 | 매수 리스트 불러오기 (load_buy_list) |
| pushButton_9 | 매수 리스트 재설정 (reset_buy_list) |
| pushButton_10 | 매도 리스트 추가 (add_sell_list) |
| pushButton_11 | 매도 리스트 불러오기 (load_sell_list) |
| pushButton_12 | 전체 자동 검색 (load_search_all_items) |
| pushButton_13 | 매도 리스트 재설정 (reset_sell_list) |

## 6. 데이터 파일 형식

### buy_list.txt / sell_list.txt
```
매수;종목코드;시장가;10;0;매수전;종목명;평가점수
```
- 구분자: 세미콜론(;)
- 평가점수는 정렬 후 추가됨

### searched_items.csv
```csv
date,매매,분류,code,종목명,PER,PBR,ROE,현재가,연중최고가,연중최저가
```

## 7. 사용 시나리오

### 기본 사용법
1. 프로그램 실행 → 키움 로그인 자동 수행
2. 조건식 목록에서 원하는 조건 선택
3. 선택된 조건으로 종목 검색
4. 검색된 종목 확인 및 매수/매도 리스트에 추가
5. 리스트 저장 및 정렬

### 자동 검색 모드
```bash
python PyCond.py exit
```
- 프로그램이 자동으로 종목 검색 수행
- 결과를 CSV에 저장 후 종료

## 8. 주의사항

### API 호출 제한
- 각 API 호출 후 0.2초 대기 (time.sleep)
- 키움 API 호출 제한을 준수하기 위함

### 파일 인코딩
- 텍스트 파일: UTF-8
- CSV 파일: EUC-KR

### 계좌 선택 로직
- 계좌번호 뒤 두자리가 '11' 또는 '10'인 계좌를 우선 선택
- 이는 위탁 계좌를 선택하기 위한 로직

### 외부 프로그램 의존성
- 뉴스 분석 기능은 별도의 BERT 모델 프로그램 필요
- 경로: `H:/급등주_bert/급등주추천_개별.py`

## 9. 확장 가능성

### 추가 가능한 기능
- 실시간 체결 알림
- 자동 매매 기능
- 백테스팅 기능
- 더 다양한 재무지표 추가
- 차트 분석 기능

### 개선 가능한 부분
- 하드코딩된 경로를 설정 파일로 분리
- 예외 처리 강화
- 로깅 기능 추가
- 멀티스레딩으로 응답성 향상

## 10. 문제 해결

### 조건식이 로드되지 않을 때
- 키움 로그인 상태 확인
- GetConditionLoad() 호출 후 충분한 대기 시간 확보

### 종목 정보가 조회되지 않을 때
- API 호출 제한 확인 (너무 빠른 연속 호출)
- 종목코드 형식 확인 (6자리, 앞자리 0 포함)

### 파일을 찾을 수 없을 때
- 실행 경로와 파일 위치 확인
- 필요한 모든 파일이 같은 디렉토리에 있는지 확인
